#
# Configuration Spring Boot
#

# Utiliser la variable d'environnement PORT si elle est définie (sinon 8080)
server.port=${PORT:8080}

#
# MySQL
# Les vraies valeurs viennent des variables d'environnement
# (DB_URL, DB_USERNAME, DB_PASSWORD)
#
spring.datasource.url=jdbc:mysql://srv2069.hstgr.io:3306/u254801366_clugit?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=u254801366_git
spring.datasource.password=Clubgit-enspdUdo13
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true